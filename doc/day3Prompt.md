接下来，我们需要监听 WM_KEYDOWN 和 WM_CHAR，要求算出方向键移动时光标的 X/Y 坐标，调用 CreateCaret 和 SetCaretPos。同时，正确处理键盘输入，使得正确显示输入的字符。 应该如何修改代码？


**第二次Prompt**
【核心任务】 请为当前程序接入标准纵向滚动条机制（WM_VSCROLL），实现长文本的平滑浏览功能。

【具体功能需求】 滚动条生命周期： 动态计算文本总高度与当前窗口客户区的高度比例。当内容超出显示范围时，自动激活并显示纵向滚动条；反之则隐藏。

交互响应：

拖拽响应： 处理 SB_THUMBTRACK 和 SB_THUMBPOSITION 消息，确保滚动块随鼠标实时移动。

步进控制： 响应点击滚动条箭头（向上/向下）以及点击空白区域（翻页）的逻辑。

滚轮支持： 接入 WM_MOUSEWHEEL 消息，实现符合操作系统惯例的鼠标滚轮缩放/滑动支持。

视图更新： 滚动时需优化重绘逻辑（如使用 ScrollWindowEx），避免由于全屏重绘导致的文字闪烁。

【质量要求】

代码风格： 逻辑模块化，关键计算公式（如 MaxScrollPos = TotalLines - VisibleLines）需附带注释。

稳健性： 严格防止滚动越界，确保滚动到文档末尾时停止，不留白屏。

**第三次Prompt**
在当前的重绘流程中，目前的逻辑需要从头遍历 Piece Table 以确定当前视口内应显示的文本起始点。当文档体量较大时，包含遍历隐藏字符开销的这种O(N)的测距循环导致了明显的渲染延迟。 请设计并实现一套基于 Piece Table 的行偏移索引优化方案，要求实现视口级的“延迟测距”： 
增量行缓存： 在 Piece Table 之上构建一个轻量级的行索引表，记录每行起始字符在 Piece Table 中的逻辑位置及 Y 轴物理坐标。 
空间检索优化：利用二分查找快速定位当前视口首行对应的 Piece 节点。
实现视口剪裁：仅对当前视口高度范围内的 Piece 段执行字符测距和 DrawText 操作。 
失效策略： 当文本发生插入或删除（Piece 增加/切割）时，仅标记受影响行之后的索引为 dirty，实现局部增量更新而非全局重构。